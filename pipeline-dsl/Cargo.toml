[package]
name = "pipeline-dsl"
version.workspace = true
authors.workspace = true
license.workspace = true
edition.workspace = true
description = "Static #[pipeline]/#[stage] DSL over the pipeline-core value layer."
repository.workspace = true
keywords.workspace = true
readme = "README.md"

[dependencies]
pipeline-core = { workspace = true }
# Exact-pinned (see [workspace.dependencies]): the macro's generated paths are
# coupled to what this crate re-exports, so the two must move in lockstep.
pipeline-macros = { workspace = true }

[dev-dependencies]
thiserror = { workspace = true }

[lib]
name = "pipeline_dsl"
crate-type = ["rlib"]
